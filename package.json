{
  "name": "mini-finance-tracker",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run prisma:generate && npm run build:workspaces",
    "build:workspaces": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "test": "npm run test -w server && npm run test -w client",
    "prisma:generate": "npm run prisma:generate -w server",
    "prisma:migrate": "npm run prisma:migrate -w server",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.7.3"
  }
}
